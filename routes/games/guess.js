const express=require('express');const router=express.Router();const game=require('../../services/games.service');
router.get('/',(req,res)=>{try{const result=req.query.gameId?game.guessNumber(req.query.gameId,req.query.number):game.newGuess();if(result.error)return res.status(400).json({status:false,creator:'FamilyBot-MD',message:result.error});res.json({status:true,creator:'FamilyBot-MD',result});}catch(e){console.error('[GUESS ERROR]',e);res.status(500).json({status:false,creator:'FamilyBot-MD',message:'No se pudo ejecutar el juego.'});}});
router.meta={title:'Adivina el número',description:'Encuentra el número secreto del 1 al 100',icon:'fas fa-hashtag',fields:[{name:'gameId',label:'ID de partida',type:'text'},{name:'number',label:'Número',type:'number'}],resultType:'json',resultField:'result'};module.exports=router;
