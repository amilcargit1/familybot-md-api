const express=require('express');const {generateExtraCanvas}=require('../../services/extraCanvas.service');const router=express.Router();const p=(r,n,f='')=>r.body?.[n]??r.query?.[n]??f;router.get('/',async(req,res)=>{try{const image=await generateExtraCanvas('game',{username:p(req,'username','Jugador'),second:p(req,'game','Juego'),value:p(req,'score','0'),detail:p(req,'detail','Resultado de partida'),footer:p(req,'footer','FamilyBot-MD')});if(String(req.query.format).toLowerCase()==='image'||String(req.headers.accept||'').includes('image/png'))return res.type('png').send(image);res.json({status:true,creator:'FamilyBot-MD',result:{url:`data:image/png;base64,${image.toString('base64')}`,format:'png'}});}catch(e){res.status(500).json({status:false,creator:'FamilyBot-MD',message:'No se pudo generar el Game Canvas.'});}});router.meta={title:'Game Canvas',description:'Resultado visual de juego.',icon:'fas fa-gamepad',method:'GET',fields:[{name:'username',label:'Jugador',type:'text',placeholder:'Nombre'},{name:'game',label:'Juego',type:'text',placeholder:'Juego'},{name:'score',label:'Puntuación',type:'text',placeholder:'100'},{name:'detail',label:'Detalle',type:'text',placeholder:'Resultado de partida'}],resultType:'image',resultField:'result.url',directImage:true};module.exports=router;