const express=require('express');const multer=require('multer');const{generateWelcomeCanvas,STYLES}=require('../../services/welcomeCanvas.service');const router=express.Router();const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:4*1024*1024,files:1}});const p=(r,n,f='')=>r.body?.[n]??r.query?.[n]??f;const t=(v,f,m)=>String(v??'').trim().slice(0,m)||f;const wantsImage=r=>String(r.query.format||'').toLowerCase()==='image'||String(r.headers.accept||'').toLowerCase().includes('image/png');
router.post('/',upload.single('avatar'),async(req,res)=>{try{const style=String(p(req,'style','neon')).toLowerCase();if(!STYLES.has(style))return res.status(400).json({status:false,message:'Estilo no válido.',styles:[...STYLES]});const avatarUrl=String(p(req,'avatarUrl','')).trim();if(!req.file&&!avatarUrl)return res.status(400).json({status:false,message:'Envía avatar o avatarUrl.'});const image=await generateWelcomeCanvas({style,avatarBuffer:req.file?.buffer,avatarUrl,username:t(p(req,'username'),'Usuario',34),groupName:t(p(req,'groupName'),'Ranking',40),members:t(p(req,'rank'),'#1',12),message:t(p(req,'score'),'1000 XP',55),date:t(p(req,'period'),'TOP',24),title:t(p(req,'title'),'RANKING',32),footer:t(p(req,'footer'),'✦ FamilyBot-MD ✦',30)});if(wantsImage(req))return res.status(200).type('png').set('Content-Length',String(image.length)).set('Cache-Control','no-store').send(image);return res.json({status:true,creator:'FamilyBot-MD',result:{url:`data:image/png;base64,${image.toString('base64')}`,format:'png',style}})}catch(e){console.error('[RANK CANVAS ERROR]',e);return res.status(500).json({status:false,creator:'FamilyBot-MD',message:'No se pudo generar el Rank Canvas.'})}});
router.meta={title:'Rank Canvas',description:'Genera una tarjeta de ranking para usuarios de un bot de WhatsApp.',icon:'fas fa-trophy',method:'POST',fields:[{name:'avatar',label:'Avatar',type:'file'},{name:'avatarUrl',label:'Avatar URL',type:'url'},{name:'username',label:'Usuario',type:'text'},{name:'rank',label:'Posición',type:'text'},{name:'score',label:'Puntuación',type:'text'},{name:'period',label:'Periodo',type:'text'},{name:'title',label:'Título',type:'text'},{name:'footer',label:'Pie',type:'text'},{name:'style',label:'Estilo',type:'select',options:[...STYLES].map(v=>({value:v,label:v})),default:'neon'}],resultType:'image',resultField:'result.url',directImage:true};module.exports=router;
